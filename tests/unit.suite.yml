# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: Unitunit
modules:
  enabled:
    - Asserts
    - \Helper\Unit
coverage:
  enabled: true
  remote: false
  include:
    - src/*
